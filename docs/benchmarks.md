# Benchmarks

The benchmark registry is maintained by `libmir` in its
[benchmark index](https://github.com/mirmir-tech/libmir/tree/main/benchmarks).
Use the index for cross-model summaries and follow its model links for complete
tables, platform metadata, and test methodology.
