[net]
git-fetch-with-cli = true

# CUDA 13 baseline; explicit environment values take precedence.
# cudarc probes bare `nvcc` unless its binding version is supplied.
[env]
CUDA_HOME = { value = "/usr/local/cuda", force = false }
NVCC = { value = "/usr/local/cuda/bin/nvcc", force = false }
CUDARC_CUDA_VERSION = { value = "13000", force = false }
LD_LIBRARY_PATH = { value = "/usr/local/cuda/lib64", force = false }
